# DatainsightAi
DatainsightAi
FastAPI  backend目录
--项目启动 uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
--退出：ctrl +c 或者 ps aux | grep uvicorn 查到进程后kill
--项目重建（不重启的情况下）--reload 参数会在代码改动后自动重建

前端启动  frontend 目录 
--项目启动  pnpm dev --host
--退出：ctrl +c 
--项目重建：项目会自动重建